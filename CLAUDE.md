# CLAUDE.md

## 1. 项目定位

本仓库当前的核心任务，不是继续把 MiroFish 做成社交媒体预测引擎，而是在原项目基础上改造成一个面向影视叙事的多智能体多结局推演系统。

你必须始终区分两件事：

- 原项目 MiroFish：现实种子信息驱动的群体智能预测引擎，偏社交媒体、舆情、平台模拟与单结局预测。
- 当前目标项目：影视剧情推演、快照、分支、多个候选结局、剧本导出与过程可追踪。

请始终把本文件视为项目的最高级工作约束。如果后续还有更细的需求摘要或技术版说明，必须与本文件一致，不能相互冲突。

## 2. 原项目真实能力

原项目的价值不在于“影视多结局”这一目标本身，而在于它已经具备的一条完整技术流水线，可被改造成叙事推演引擎：

1. 图谱构建：从种子材料中抽取实体、关系、本体与图谱。
2. 环境搭建：根据图谱实体生成 Agent Profile 和模拟参数。
3. 社交平台模拟：基于 OASIS / camel-ai 运行 Twitter、Reddit 等群体模拟。
4. 报告生成：通过 Report Agent 和工具调用输出总结性结果。
5. 深度互动：与模拟世界中的 Agent 或报告 Agent 继续对话。
6. 动态上下文记忆：借助 Zep 和图谱记忆持续更新角色状态。

当前仓库的实际技术栈是 Flask + Vue 3 + Vite + Python 工具链，不要把它误写成 FastAPI/React 项目。

## 3. 当前改造目标

当前目标是把原项目改造成一个可以服务影视推演的系统，重点能力如下：

- 影视剧情解析：把输入材料转成符合叙事结构的角色、事件、关系、冲突、动机、世界观与时间线。
- 快照：把当前剧情状态、角色状态、公共记忆和已发生事件保存为可回滚中间状态。
- 分支：从快照点派生候选剧情路径，继续推演不同走向。
- 中途智能评价：在推演过程中对局部结果做评价，作为智能快照和分支选择依据。
- 智能分支评分：对分支做叙事连贯性、角色一致性、冲突强度、主线贡献度和观赏性评分。
- 多结局对比：输出多个可比较的候选结局，而不是只返回一个结果。
- 剧本导出：将选定结局整理成可阅读、可提交、可展示的最终文本。
- 过程可追踪：记录快照链路、分支依据、评分结果与修改过程。

## 4. 最高优先级硬约束

以下约束是硬规则，不是建议：

### 4.1 快照必须前后端一致

- 任何快照能力都必须同时考虑后端、前端和持久化层。
- 如果后端新增了快照能力，前端必须能看到、能触发、能回放、能管理。
- 如果需要数据库、文件存储、状态表或额外索引结构，必须一并补齐。
- 不允许出现“后端已经保存快照，但前端没有入口或看不见”的半成品。

### 4.2 禁止社交媒体化偏移

- 所有提示词、Agent 生成逻辑、关系抽取逻辑和 UI 语义都必须默认服务于影视推演。
- 不要把环境、灾难、道具、世界观因素误处理成社交账号。
- 不要把整个系统再次做成“平台用户互动模拟器”。
- 如果某个字段或提示词会把系统推回社交媒体语义，必须优先修正。

### 4.3 关系与实体类型必须分级

- 角色、NPC、组织、环境、事件、灾难、道具、背景知识、公共记忆必须明确区分。
- 某种灾难被抽成实体，在部分情况下可以接受；但它作为社交账号是不可接受的。
- 环境因素如果无法成为独立实体，可以进入背景知识或公共记忆，而不是硬造 Agent。
- 所有实体类型都要有明确的可用范围和禁止范围。

### 4.4 必须处理主次关系

- 所有角色都要有主线影响因子、剧情相关度、与核心角色的联系强度、干扰主线风险等评价信息。
- 不允许 NPC 因为文本里出现很多次就自动变成主线驱动者。
- 需要有机制压制边缘角色带偏主线。
- 角色和剧情关系要按重要性、关联度和影响范围分层。

### 4.5 允许人工干预

- 必须支持手动修改 Agent。
- 必须支持中途修改群体记忆或公共记忆。
- 必须支持与单个 Agent 单独对话，用于纠偏、诊断和调试。
- 人工干预不是补丁，而是调试和控制剧情方向的重要功能。

### 4.6 中途智能评价必须存在

- 智能快照和分支不能孤立存在，必须依赖中途评价结果。
- 中途评价用于判断是否分叉、是否继续、是否回滚、是否合并。
- 如果没有中途评价，就不要假装已经具备智能快照和智能分支。

### 4.7 必须写清楚原项目与新目标的区别

- 必须在项目说明中简要说明原项目是社交媒体单结局预测引擎。
- 必须明确当前目标是影视多结局推演。
- 不允许让新来的模型误以为原项目本来就是影视多结局系统。

### 4.8 必须用 git 管理并允许多个 commit

- 所有开发必须用 git 管理。
- 允许并鼓励多个小 commit，而不是一次大改到底。
- 每个 commit 应尽量只解决一个局部问题。
- 允许先做结构化规划，再分阶段实现，不要把“提交次数少”当成目标。

### 4.9 必须记录计划、思路和验收方式

- 每次较大的修改前，先写计划。
- 每次较大的修改中，记录实现思路和关键取舍。
- 每次修改后，记录验证方式和结果。
- 不要只写代码，不写理由。

## 5. 推荐的项目结构认知

在理解这个仓库时，默认按以下层次思考：

1. 输入层：影视文本、剧情摘要、角色关系、世界观材料。
2. 解析层：实体抽取、关系抽取、叙事结构识别、公共记忆整理。
3. 生成层：角色配置、Agent 生成、记忆注入、剧情状态生成。
4. 推演层：中途评价、快照、分支、分支评分、路径比较。
5. 展示层：结局对比、快照浏览、手动修改、单 Agent 对话、导出。

任何新功能都必须先判断它属于哪一层，不要把层之间的职责混在一起。

## 6. 可以复用的部分

以下能力适合保留或改造，不要轻易推翻：

- 图谱构建思路，可以迁移为影视叙事状态图。
- Zep 动态上下文和角色记忆能力。
- 自动分析实体关系并生成智能体的流程。
- 记忆检索、历史回放和关系更新管线。
- Agent Profile 生成流程。
- Report Agent 的工具调用框架，可迁移为结局分析和解释模块。
- 前端分步骤工作流。
- 后台任务、日志、进度条、异步处理。

## 7. 必须重做的部分

以下内容不是简单改名，而是要重写：

- 影视剧情解析器。
- 叙事实体类型体系。
- 快照和分支管理。
- 中途评价与分叉判断。
- 分支评分与排序。
- 多结局比较与选择。
- 最终剧本导出。
- 面向影视推演的前端交互。

## 8. 开发优先级

默认按这个顺序推进：

1. 先校准项目定位和数据语义边界。
2. 再做快照和分支的最小可运行闭环。
3. 接着加入中途智能评价和分支评分。
4. 再做多结局展示、比较和选择。
5. 最后补剧本导出、可视化和更复杂的搜索逻辑。

如果某个功能会绕开前面的基础边界，不要提前做。

## 9. 实现原则

- 先做最小闭环，再逐层扩展。
- 先保证语义正确，再追求功能完整。
- 先让前后端可见可用，再谈自动化优化。
- 先把 Agent 的职责边界写清楚，再开始大规模生成。
- 遇到与影视推演目标冲突的实现，要优先纠正方向，而不是硬兼容。

## 10. 代码修改规则

- 所有修改优先围绕当前目标闭环展开，不要顺手重构无关模块。
- 遇到跨模块修改，先写清影响面和回退方案。
- 如果某项功能依赖数据库、接口或前端状态，必须同步检查三端一致性。
- 如果修改会影响现有行为，必须说明会影响什么、为什么改、如何验证。
- 不要为了统一风格而破坏现有可用流程。

## 11. 原项目和本项目的运行方式

### 11.1 环境准备

- Node.js 18+
- Python 3.11 到 3.12
- uv

### 11.2 安装依赖

在仓库根目录执行：

```bash
npm run setup:all
```

也可以分开执行：

```bash
npm run setup
npm run setup:backend
```

### 11.3 启动开发环境

在仓库根目录执行：

```bash
npm run dev
```

默认会同时启动前后端。

### 11.4 单独启动

```bash
npm run backend
npm run frontend
```

### 11.5 端口约定

- 前端默认运行在 `http://localhost:3000`
- 后端默认运行在 `http://localhost:5001`

### 11.6 后端启动入口

后端由 `backend/run.py` 启动，Flask 应用工厂在 `backend/app/__init__.py`。

### 11.7 配置要求

通常需要在根目录准备 `.env`，至少包括 LLM 和 Zep 相关配置。若新增存储、数据库或快照依赖，也必须同步补充说明。

## 12. 对 Agent 的工作要求

- 先确认目标，再改代码。
- 先确认数据边界，再改提示词。
- 先确认前后端链路，再做功能闭环。
- 先写计划，再执行。
- 先验证，再扩大范围。
- 遇到不确定问题，优先做局部检查，不要直接大范围重写。

## 13. 输出要求

如果后续要在这个仓库里继续写代码或重构，请默认按照下面顺序输出思考结果：

1. 目标是什么。
2. 受影响的模块是什么。
3. 方案是什么。
4. 如何验证。
5. 有哪些风险或未覆盖项。

不要只给结论，不给边界和验证方式。

## 14. 结束语

这个仓库不是一个简单的“把旧项目改个名”的工程，而是一次从社交媒体预测引擎向影视多结局推演系统的语义迁移。所有实现都必须围绕这个方向推进。