# CLAUDE.md

## 项目定位

本仓库当前的任务，不是复刻原始 MiroFish，而是把它改造成一个面向影视叙事的多智能体结局协同推理系统。

请始终把 [基于MiroFish+20254003027+袁沛希_技术版.md](基于MiroFish+20254003027+袁沛希_技术版.md) 作为本项目的需求摘要和目标定义。

在实用过原项目后，已经确认两类能力值得尽量保留：一是基于 Zep 动态上下文的角色扮演效果，二是自动分析实体关系并生成智能体的流程。整体改造策略应当偏向小步调整，先保留原项目的大体代码结构，再围绕新的结局推理目标逐层增加能力。

## 原项目 MiroFish 的真实能力

MiroFish 的核心定位是“现实种子信息驱动的多智能体预测引擎”，不是影视结局推理器。它的工作流更接近一套完整的技术实现链路：

1. 图谱构建：从现实材料中抽取种子信息，做实体、关系、本体和图谱构建。
2. 环境搭建：读取图谱实体，生成 Agent Profile 和模拟参数。
3. 开始模拟：基于 OASIS / camel-ai 运行 Twitter、Reddit 等平台的群体模拟。
4. 报告生成：用 Report Agent 通过 ReAct + 工具调用生成预测报告。
5. 深度互动：与模拟世界中的 Agent 或 Report Agent 对话。
6. 动态上下文角色扮演：利用 Zep 提供的图谱记忆和上下文检索，持续调整 Agent 的人设、行为和回答风格。

它的技术栈以 Flask + Vue 为主，后端围绕 Zep、OASIS、camel-ai、LLM 工具链展开。对我们来说，原项目最有价值的不是某个单点模型，而是整条可复用的技术流水线。

## 表述口径

### 原项目怎么写

原项目部分要尽量写成技术实现说明，重点描述它已经具备的能力：OASIS 双平台模拟、Zep 动态上下文、实体关系抽取、Agent Profile 自动生成、Report Agent 的 ReAct 工具链、后台任务与前后端工作流。

### 目标项目怎么写

目标项目部分要尽量写成目标和能力说明，不要过早锁死技术细节。重点讲清楚影视结局协同推理要解决什么问题、最终要产出什么结果、用户怎么用，而不是把每个模块的实现方式一次性定死。

可写得更笼统一些的方向包括：结构化解析、记忆增强、分支搜索、评分排序、可视化比较和结果导出，这样后续实现可以保留模型发挥空间。

## 关键目标

本项目当前最关键的目标，不是一次性重构整个系统，而是先建立一个能在原项目基础上逐步生长的结局推理闭环：

- 快照：把当前剧情状态、角色状态和已发生事件保存成可回滚的中间状态。
- 分支：从快照点生成候选剧情分支，支持继续推演不同路径。
- 智能快照：让系统自动识别值得分叉的关键节点，而不是人工在每一步都介入。
- 智能分支评分：对分支做合理性、冲突强度、角色一致性和观赏性等综合打分。
- 多个结局供用户选择：输出多个可比较的候选结局，而不是只给一个单点答案。
- 导出最终剧本：把选定结局整理成可直接阅读、可提交、可展示的最终脚本或剧本稿。
- 过程可追踪：保留日志、快照链路和分支依据，方便回放和解释。

## 目标项目和原项目的差距

### 1. 任务目标不同

原项目处理的是“现实世界未来推演”和“群体行为预测”，你的目标是“影视剧情的多路径结局推理”。

这意味着输出对象不同：

- 原项目输出的是预测报告、模拟世界和交互式 Agent 对话。
- 目标项目输出的是候选结局、分支树、角色一致性分析和结局评分排序。

### 2. 数据结构不同

原项目依赖实体图谱、舆情或现实种子材料、Agent Profile、平台事件流。

目标项目需要的是叙事结构：角色、事件、冲突、目标、动机、时序、因果链、世界书、角色卡。

### 3. Agent 语义不同

原项目的 Agent 更接近现实世界社会角色或平台用户，强调社会演化和互动。

目标项目的 Agent 必须是叙事角色分工：导演代理、编剧代理、角色代理、观众代理。

### 4. 推理方式不同

原项目主要是时间推进式模拟和报告总结。

目标项目需要分支搜索式推理，重点是：

- 候选剧情分支生成
- MCTS / 搜索树扩展
- 剪枝和去重
- 多维评分
- 结局排名与解释

### 5. 评价方式不同

原项目的评价偏向模拟结果、预测发现和工具检索质量。

目标项目的评价要围绕叙事质量：连贯性、角色动机一致性、情节张力、结局满意度。

### 6. 技术栈存在不一致

你的技术版摘要里曾写到 FastAPI / React / Vue，但原项目实际是 Flask + Vue + Python 工具链。

如果要沿用原项目，必须明确：

- 要么保留 Flask，不要半路再引入一套新的后端框架。
- 要么明确做架构迁移，但不要把 Flask 和 FastAPI 混成一层抽象。

## 可以复用的部分

这些模块适合改造后复用，不要从零推翻：

- 图谱构建思路：可迁移为“影视叙事状态图”构建前置流程。
- Zep 动态上下文驱动的角色扮演：可以保留，不必强行改成更耗 Token 的注入式方案。
- 自动分析实体关系并生成智能体：可以保留，这是很适合当前目标的基础能力。
- Zep / GraphRAG 类记忆管线：可迁移为角色记忆、历史行为召回和剧情状态检索。
- Agent Profile 生成器：可迁移为角色配置和行为偏好生成器。
- Report Agent 的 ReAct 工具调用框架：可迁移为“结局分析 / 结局总结 / 结果解释”模块。
- 前端分步骤工作流：可迁移为“剧情输入 -> 状态整理 -> 分支推理 -> 结局对比 -> 导出”。
- 日志、任务状态、进度条、后台异步任务：这些都可以保留。

## 必须重做的部分

以下内容不是简单改名，而是新能力：

- 影视剧情解析器：要重新定义输入、实体类型和叙事关系。
- 快照和分支管理：要有明确的状态保存、恢复和派生机制。
- 智能快照选择：要从剧情中识别哪些节点值得分叉，哪些节点应该继续线性推进。
- 分支评分与排序：需要按叙事质量和用户目标重写。
- 多结局比较与用户选择：要有结局候选集、排序与展示逻辑。
- 最终剧本导出：要把推演结果整理成结构清晰、便于阅读和交付的成稿。
- 结局对比 UI：需要新的可视化和交互，而不是现成模拟面板。

## 当前开发优先级

按这个顺序推进，最稳：

1. 先保留原项目主流程，把结局推理能力放到可插拔的新层里。
2. 做快照和分支的最小闭环，先让系统能保存、恢复和派生剧情状态。
3. 接入智能快照和分支评分，让分叉点和候选路径开始可控。
4. 做多个结局的生成、排序与用户选择。
5. 做最终剧本导出和解释输出。
6. 最后再考虑更复杂的搜索、可视化和增强式交互。

## 工作原则

- 不要把 MiroFish 的“预测报告”误写成“影视结局推理”。
- 不要默认原项目已经有快照、分支评分、多结局选择和剧本导出这些能力。
- 任何新功能都要先判断：它是能复用原项目，还是必须新写。
- 如果实测已有 Zep 角色扮演和自动智能体生成效果，就优先保留，不要为了形式统一去做高成本重构。
- 如果是跨模块改造，优先做最小闭环，不要一次性重构整个系统。
- 新增代码时，优先保持模块边界清晰：原有模拟、快照分支、评分排序、导出展示分开。

## 给后续实现者的约束

- 任何分析、设计或代码修改，都要围绕“影视结局协同推理”这个目标展开。
- 如果引用 MiroFish 原项目能力，要明确区分“可复用”和“需要重做”。
- 如果后续补充新的技术方案，优先写进这份 CLAUDE.md 或同步更新技术版摘要。
- 如果要开始编码，先做一个最小可运行闭环，而不是先追求完整大而全架构。
- 目标描述可以偏功能，技术实现可以笼统，避免过早把实现路径锁死。
